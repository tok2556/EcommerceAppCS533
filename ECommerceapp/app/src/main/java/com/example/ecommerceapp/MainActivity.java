package com.example.ecommerceapp;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;

public class MainActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);

        //code here to initialize the app, for example load product data from the json webservice and desplay in in a list view
    }
    // Code to handle user actions, for example, when the user clicks on a product in the list view.
}